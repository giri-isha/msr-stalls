# The TEST database. Its name MUST end in _test — the suite refuses anything else
# because it truncates every table between tests. Create it once:
#   createdb msr_stalls_test
DATABASE_URL="postgresql://postgres:PASSWORD@localhost:5432/msr_stalls_test?schema=public"
