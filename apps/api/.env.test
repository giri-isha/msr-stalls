# The TEST database. Its name MUST end in _test — the suite truncates every
# table between tests and refuses to run against anything else.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/msr_stalls_test?schema=public"
